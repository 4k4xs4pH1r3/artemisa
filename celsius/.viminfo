# Este fichero viminfo ha sido generado por Vim 6.1.
# Se puede editar ¡si se tiene cuidado!

# Valor de 'encoding' cuando se escribió este fichero
*encoding=utf-8


# hlsearch on (H) or off (h):
~H
# Historia de Línea de mandatos (de lo más nuevo a lo más antiguo):
:wq

# Historia de Cadena de búsqueda (de lo más nuevo a lo más antiguo):

# Historia de Expresión (de lo más nuevo a lo más antiguo):

# Historia de Línea de entrada (de lo más nuevo a lo más antiguo):

# Historia de Línea de entrada (de lo más nuevo a lo más antiguo):

# Registros:

# Marcas en el fichero:
'0  1  2  ~/sandra

# Lista de saltos (el más reciente, el primero):
-'  1  2  ~/sandra

# Historia de las marcas en los ficheros (de la más reciente a la más antigua):

> ~/sandra
	"	1	2
	^	1	3
	.	1	2
