IMPORTANTE: El instalador incluido aqui solo instala el software, no actualiza versiones viejas. El actualizador está en proceso
de produccion. 

El directorio instaladorcelsius debe estar ubicado dentro del arbol web. Una vez hecho esto, y con el servidor ejecutándose,
acceda a dicho directorio usando algun browser (por ejemplo, www.midireccion.com/instaladorcelsius) y siga las instrucciones para instalar el software CELSIUS.
El directorio CELSIUS posee todos los archivos que conforman dicho software. Este directorio tambien debe colocarse dentro del
arbol web. El nombre que se le asigne a dicho directorio cuando lo copia será luego solicitado durante la instalación de CELSIUS.


IMPORTANT: The installer included here installs the software only, it doesn't update old versions. The updater is right now been developped.

The instaladorcelsius directory must be placed inside the web tree. Once it's been done, and with the web server running,
you need to acces to that directory using any browser (i.e., www.myaddress.com/instaladorcelsius) and follow the instructions in order to install the software CELSIUS.
CELSIUS directory has all the files of the software. This directory must be placed inside the web tree too. The name you assign to this directory when it's copied
will be used during the instalation.